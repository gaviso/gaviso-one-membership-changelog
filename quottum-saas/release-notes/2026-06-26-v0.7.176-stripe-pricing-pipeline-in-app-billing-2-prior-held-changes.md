---
title: "v0.7.176 — Stripe pricing pipeline + in-app billing + 2 prior held changes"
description: "Bundle of three held releases (v0.7.174 → v0.7.176). Closes the Stripe pricing gap (drift, sync-all, subscriber migration, history, reverse-sync) AND brings billing in-app (invoice"
publishedAt: 2026-06-26T05:06:42Z
tags:
  - Release
---

Bundle of three held releases (v0.7.174 → v0.7.176). Closes the Stripe pricing gap (drift, sync-all, subscriber migration, history, reverse-sync) AND brings billing in-app (invoices + payment method via Stripe Elements + upgrade/downgrade with explicit proration policy). Also several smaller wins.

## 🚨 Operator action required after deploy

Two reasons you'll want to click into Stripe right after this lands:

1. **Stripe webhook needs 2 new events** — open Stripe Dashboard → Developers → Webhooks → your endpoint → add `price.updated` and `price.deleted`. Without them, edits made directly in Stripe Dashboard won't reverse-sync into our `plan_price_history` audit. The in-app setup checklist on `/admin/billing` already shows the updated list of 9 events.
2. **Re-sync the plans to push corrected prices to Stripe.** v0.7.175 changed the Stripe sync to use `plans.prices.year` directly instead of the derived `yearlyFromMonthly` (which was ~17% off for the actual $290/yr and $790/yr you have configured). Open `/admin/integrations` → click **Sync all** to push the corrected yearly Prices.

## 🧪 Stripe configuration visibility (v0.7.174)

- `/admin/integrations` Stripe card now reads **`Configured (test mode)`** (blue) instead of the misleading **`Configured (off)`** when set up in test mode. Live stays **`Connected`** (green).
- `/admin/billing` Topbar now shows **`Test mode`** / **`Live mode`** chip — every KPI + activity feed below it is mode-dependent.
- Per-row `test` / `live` chip on the Billing actions feed (was already there on Webhook events).

## 📊 Feature matrix + Stripe sync (v0.7.175)

- Feature matrix column headers now stack **every cadence the plan has a price for** (e.g. `$29/mo` + `$290/yr`) instead of only the cheapest.
- Stripe sync uses `plans.prices.year` directly when set; falls back to `yearlyFromMonthly(-20%)` only for legacy plans without the prices map.
- Stripe yearly price nickname now shows the **actual** discount % vs 12× monthly (your $290/yr is ~17% off, not the hardcoded "20% off").
- Consolidated 3 copies of `calcRevenue` into [`src/lib/contract-revenue.ts`](https://github.com/gaviso/quottum-saas/blob/v0.7.176/src/lib/contract-revenue.ts) with `semiannual` handled correctly across the board.

## 💰 Stripe pricing pipeline (v0.7.176)

| Surface | What's new |
|---|---|
| `/admin/plans` per-card | Yellow **"Stripe out of sync — click Sync"** pill when DB prices ≠ Stripe Price amounts (per cadence) |
| `/admin/integrations` | **Sync all** button (sequential, aggregated result); per-plan **Migrate subs** button (dry-run → confirm → real migration, `proration_behavior: 'none'`) |
| Webhook handler | New `price.updated` + `price.deleted` cases — reverse-sync writes `plan_price_history` rows with `source='webhook'` |
| PATCH `/api/admin/plans/[id]` | Writes a `plan_price_history` row when prices actually change |
| New migration | [`070_plan_price_history.sql`](https://github.com/gaviso/quottum-saas/blob/v0.7.176/supabase/migrations/070_plan_price_history.sql) — append-only audit, RLS to platform admins |

## 💳 In-app billing (v0.7.176)

- **Invoices list** at `/dashboard/settings/billing` — reads from Stripe directly (no DB caching), past 25 invoices. **View** opens Stripe's hosted invoice; **Download PDF** opens Stripe's signed `invoice_pdf` URL. Owner/admin only.
- **Update payment method** — Stripe Elements + SetupIntent flow inline. Card details never touch Quottum (PCI scope stays SAQ A). On success, sets the customer's default + the active subscription's `default_payment_method` so the next renewal uses the new card.
- **Upgrade/downgrade** via new `POST /api/billing/change-plan` endpoint that bypasses Stripe Portal so we control proration per direction:
  - **Upgrade** → `proration_behavior: 'create_prorations'` + `billing_cycle_anchor: 'now'` (charge difference immediately, reset cycle)
  - **Downgrade** → `proration_behavior: 'none'` (no credit, change at next billing — conservative default, avoids surprise customer credits)
  - **Lateral cadence swap** → prorate
- Existing Stripe Portal **Manage billing** button kept as a fallback for tax IDs, billing email, etc.

## 🧹 Beta feedback

`archived` status added to BetaManager — for "we read it, no action coming, but keep around". Schema already accepted any text; just code-side.

## Dependencies

Added `@stripe/stripe-js` + `@stripe/react-stripe-js` (route-code-split, only loads on `/dashboard/settings/billing`).

## Database

One new migration — `070_plan_price_history`. Append-only audit table, safe for live data, RLS to platform admins. Already applied.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
